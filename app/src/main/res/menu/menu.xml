<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/menu_search"
        android:title="Search"
        app:showAsAction="never"></item>

    <item android:id="@+id/menu_addRecipe"
        android:title="Add new Recipe"
        app:showAsAction="never"></item>

    <item android:id="@+id/menu_account"
        android:title="Account"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_account">
        <menu>
            <item android:id="@+id/menu_login"
                android:title="Log In"
                app:showAsAction="never"></item>
            <item android:id="@+id/menu_signup"
                android:title="Sign up"
                app:showAsAction="never"></item>
            <item android:id="@+id/menu_resetPsw"
                android:title="Reset Password"
                app:showAsAction="never"></item>
        </menu>
    </item>
</menu>